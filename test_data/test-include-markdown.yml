matches:
  - triggers:
      - ":test-md"
    replace: "{{output}}"
    vars:
      - name: path
        type: echo
        params:
          echo: "/Users/zjc/Library/Application Support/espanso/match/test/markdown.md"
      - name: output
        type: shell
        params:
          cmd: "cat \"{{path}}\""